你是一名资深的客服质量评估专家。现在需要你对一条【自动回复】进行多维度评分。

【参考标准】
请严格对照以下人工参考回复（Human Reference）和备注分析（Annotator Notes）来判断自动回复的质量。

<Human_Reference>
{human_reference}
</Human_Reference>

<Annotator_Notes>
{annotator_notes}
</Annotator_Notes>

【用户问题】
{user_question}

【待评估的自动回复】
{auto_reply}

【评分任务】
请根据以下四个维度，输出 1-5 分的评分（幻觉维度为 0/1），并以合法 JSON 格式返回。
评分细则（务必严格遵守）：

1. factual_accuracy (1-5)：信息准确性
   - 检查自动回复中的事实（产品名、价格、规则、流程节点）是否与 Human_Reference 一致。
   - 5=完全正确且完整；4=极少数次要细节偏差；3=有一处明显事实错误；2=多处错误；1=核心事实完全错误。

2. problem_resolution (1-5)：问题解决度（强化主动服务判定）
   - 检查自动回复是否主动帮用户解决问题，还是仅仅提供通用信息让用户自己去操作。
   - 关键判定：如果人工参考回复中包含"帮您查"、"提供订单号/商品名我来处理"、"我帮您申请"等主动代劳动作，而自动回复仅给出规则罗列或让用户"查看详情页/联系客服"，则最高只能给 3 分。
   - 5分：主动代劳，给出确定性结论或直接帮用户操作（如"我帮您查订单"）。
   - 4分：主动追问缺失信息（如"请告知订单号"）并承诺处理。
   - 3分：给出了正确的通用规则/路径，但用户仍需自己动手操作（"正确但没用"）。
   - 2分：答非所问或遗漏核心诉求。
   - 1分：完全无用。

3. politeness (1-5)：礼貌与亲和度
   - 语气是否温和、礼貌，有无共情表达。
   - 5=非常礼貌且有共情（如"非常理解您的困扰"）；4=得体礼貌；3=中性机械；2=生硬冰冷；1=粗暴或命令式。

4. hallucination_flag (0 或 1)：幻觉标记（硬指标）
   - 0 = 回复中每一条实质性信息都能在 Human_Reference 或显式常识中找到依据。
   - 1 = 回复中存在 Human_Reference 中未提及、无法由上下文推理出的全新"事实性捏造"（编造了活动、赔偿、日期、功能等）。

【输出格式】
请只输出纯 JSON，不要包含 Markdown 代码块标记或其他解释文字。
格式示例：
{{"factual_accuracy": 4, "problem_resolution": 5, "politeness": 3, "hallucination_flag": 0, "reason": "简要说明扣分或满分理由"}}