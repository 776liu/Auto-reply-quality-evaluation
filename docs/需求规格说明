1. 项目目标与范围
1.1 业务目标
    决策支撑：通过量化评估，判断当前自动回复功能是否具备扩大覆盖范围的条件。
    质量透视：建立可解释的质量视图，明确“好回复好在哪，差回复差在哪”。
    工程前置：为后续转为“定期自动化巡检”提供标准化的评估基线。

1.2 评估范围
    数据来源：在线客服场景下的 20 组真实对话数据。
    评估对象：系统生成的 auto_reply（自动回复）。
    对照基准：人工坐席撰写的 human_reply 及对应的 analysis（标注分析）。
    评估维度：覆盖业务方提出的“准确、有用、语气好、不瞎编”四项要求。

------------------------------------
2. 评估指标定义（核心）
2.1 指标一：“准确”
    属性	    内容

    定义	    自动回复中的事实性信息（规则、价格、路径、功能状态）与人工参考回复保持一致，无错误或矛盾。

    量化方式	5 分制（区间评分）。由 LLM-as-Judge 对照 human_reply 进行打分。

    评分锚点	
        5分：完全正确，核心与细节均无误；
        3分：存在一处明显事实偏差但不影响主干；
        1分：核心事实完全错误（如把“退款”说成“换货”）
    选择理由	事实错误会直接损害品牌信任，是客服场景的“命门”，必须作为独立硬指标量化。


2.2 指标二：“有用”
    属性	    内容

    定义	    回复是否直接、确定性地解决了用户当前问题，提供了可执行的下一步动作或最终答案。

    量化方式	5 分制（区间评分）。重点考察“是否终结用户追问”的闭环程度。

    评分锚点	
        5分：给出具体操作步骤或明确结论（如“已为您提交工单，编号123”）；
        3分：给出方向但需用户二次追问（如“请联系售后”）；
        1分：完全答非所问。

    选择理由	这是客服存在的直接价值，与“准确”构成了质量的双核心。


2.3 指标三：“语气好”
    属性	    内容
    
    定义	    措辞是否礼貌、温和，是否包含共情表达，是否符合客服行业规范。

    量化方式	5 分制（区间评分）。基于关键词、句式（疑问/建议）和情感色彩综合判定。

    评分锚点	
        5分：包含共情词汇（“非常理解”、“很抱歉”），语气柔和；
        3分：中性、机械，无冒犯但无温度；
        1分：命令式或冷冰冰（如“自己看说明书”）。

    选择理由	虽非功能核心，但直接影响客户体验（NPS），且语气好坏与模型Prompt强相关，便于后期调优。

2.4 指标四：“不瞎编”
    属性	    内容

    定义	    回复中是否存在人工参考回复中未提及、且无法由常识推理得出的“无依据信息”。

    量化方式	二值判定（0/1）。0代表无幻觉（通过），1代表存在幻觉（不通过）。

    判定依据	逐句核查自动回复中的实质陈述，若无法在 human_reply 中找到直接依据或等价推理，即判为幻觉。

    选择理由	这是安全红线。一旦出现幻觉，即使前3项得分高，该回复也属于“不可用”范畴。作为底线指标单独监控。

----------------------------------
3. 指标优先级与综合评分规则
3.1 优先级排序（由高到低）
    红线级（P0）：不瞎编（Grounding）—— 一旦触发幻觉标记，该回复自动进入“高风险”名单。

    核心级（P1）：事实准确性 + 问题解决度 —— 决定回复是否能用。

    优化级（P2）：礼貌亲和度 —— 决定体验好坏。

3.2 综合得分计算公式（加权）
    综合得分 = (事实准确性 × 0.35) + (问题解决度 × 0.35) + (礼貌亲和度 × 0.20) + (无幻觉奖励 × 0.10)

    其中 无幻觉奖励 = 1 - hallucination_flag（即若无幻觉计1分，若有幻觉计0分）。

    补充惩罚规则：若 hallucination_flag = 1，则最终综合得分直接乘以 0.6，以体现“一票否决”的严重性。

------------------------------------
4. 技术方案与评估流程
4.1 核心策略：LLM-as-Judge
    采用高性能大语言模型（SiliconFlow / DeepSeek-V3）作为统一评判员。原因如下：

    一致性：避免人工评分的主观波动。

    可解释性：可要求模型输出评分理由，便于追溯。

    效率：20条数据可在15分钟内完成全维度评分。

4.2 评估流水线逻辑
    输入层：加载 auto_replies.json 和 human_ref.json。

    Prompt 拼接层：将 {user_question}、{auto_reply}、{human_reply}、{analysis} 注入预设的标准化模板。

    推理层：调用 LLM API，设置 temperature=0.1 以保证评分确定性。

    解析层：强制要求返回结构化 JSON（含四个维度的分数及理由）。

    后处理层：依据公式计算综合得分，生成分布统计，筛选最差3条 Case。

    报告层：输出估报告。

---------------------------------------------------

5. 局限性分析与风险预案
    潜在风险	                   影响分析	                                            应对/改进策略
    LLM 评分偏见	    模型可能对特定措辞（如数字、长句）有过严或过宽的倾向。	 对比人工标注的 analysis 做调优，必要时引入 Few-shot 示例。
    单轮上下文不足	     20条数据均为单轮对话，若真实场景含多轮，评估可能失准。	 明确当前评估仅限“单轮首响”，后续迭代可引入对话历史拼接。
    人工参考回复质量	 若 human_reply 本身就有错误，评估基准会失效。	        在报告中备注异常 Case，建议业务方抽检人工标准。
    幻觉判定边界	     常识性内容（如“请您登录官网”）是否算幻觉？	            在 Prompt 中明确“显式业务信息”才判幻觉，通用操作话术不标记。

----------------------------------
6. 后续自动化扩展建议
建立基线数据库：将本次20条评分结果作为基准，后续新版本自动回复需与基线对比。

增加知识库检索：当没有人工参考回复时，改用 RAG 从文档中检索事实依据，扩展评估泛化能力。

异常告警：当幻觉率超过 5% 或综合得分低于 3.0 时，自动触发告警，阻止覆盖扩大。

